services:
  - type: web
    name: japanese-word-search
    env: python
    region: oregon
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      bash setup_data.sh

    startCommand: "python app.py"
    autoDeploy: true
